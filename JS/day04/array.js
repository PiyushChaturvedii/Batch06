// Introduction of Array

// premitive non-premitive datatypes

// premitive - immutable value cant change directly
//boolean, numbers, string, null

//non-preitive - mutable, value can change directly, store as a refernce in memory
// array, object, functions

// What is array

//collection of any like string, boolean, number, complex datatype

let oddNumbers = [1,3,5,7,9]; //array
console.log(Array.isArray(oddNumbers));
console.log(typeof oddNumbers);

let fruits =['banana','apple','mangos'];

let mixedArray = [1,1.3,'banana','apple','cat',2n];

// 0 base indexing
console.log(mixedArray[3]);
